# CLAUDE.md

> 本文件是本项目的协作约定，供 Claude（及开发者）遵守。

## 项目概述

**印前文件服务器** —— 面向家纺/床上用品印前环节的 Web 服务。
上传设计图 → 选类型尺码 → 生成分层印前文件 → 入文件库；独立的排版功能从文件库或上传取图，按排版配置拼版输出印刷文件。

技术栈：Python 3.10+ / FastAPI / Jinja2（前后端不分离）/ pyvips（主图像引擎）/ psd-tools（分层）/ Pillow（缩略图）/ Docker 部署。

详细设计见 `docs/` 下各文档。

## 当前阶段

- 规划与文档阶段，**先不写业务代码**
- 当前任务：验证分层 TIF / PSB 写入可行性（技术 spike）
- 文档进展见 `README.md` 文档清单

## 工作规则

### 1. 文档优先
- 需求与设计变动**先改文档，再动代码**
- 新增模块/功能前，确保对应 `docs/` 文档已写或同步更新
- 文档用中文，代码注释用中文

### 2. 不擅自实现
- 用户说"先不写代码"时，只产文档与验证脚本
- 验证脚本（spike）放在 `spike/` 目录，不混入正式 `app/`
- 架构性决策（选型、模块边界、技术风险）必须先告知用户并确认

### 3. 技术栈约束
- **图像处理主力用 pyvips**，大图/高位深/ICC 由它负责
- **分层用 psd-tools**，Pillow 仅做缩略图与轻量 I/O
- **OpenCV 不进主链路**，仅 QC 预检只读分析用
- **输出分场景**：印前生成用 PSD（分层）；排版输出用普通 TIF（不分层，带透明 alpha）
- **pyvips 写 TIF 坑**：`Image.black + 常量` 运算会丢 interpretation，默认存成 32位浮点 scRGB，Photoshop 报"不受支持的位深度"。保存前必须 `.colourspace("srgb").cast("uchar")` 强制 8位 sRGB，并加 `bitdepth=8`
- 网页展示**全部用缩略图**，原图不直接进页面
- 配置用 JSON；印前配置树与排版配置相互独立

### 3a. 依赖与外部插件管理
- 项目外部依赖、系统级库、第三方插件统一放在 **`vendor/`** 目录，便于集中管理
- 例：`vendor/libvips/` 存放 libvips 的 Windows 预编译包
- Python 包用 `uv` 管理虚拟环境（`.venv/`），与 `vendor/` 区分：
  - `.venv/` —— pip/uv 安装的 Python 包
  - `vendor/` —— 非 pip 管理的外部二进制/插件
- spike 脚本与正式代码通过环境变量（如 `PATH`、`PYVIPS_LIBRARY`）指向 `vendor/` 下的库

### 4. 风险意识
- 分层 TIF / PSB 写入是**最高风险点**，需 spike 验证后再定方案
- 涉及外部依赖（libvips 系统级、psd-tools 能力边界）的方案，先验证假设再承诺

### 5. 代码风格（待实现时生效）
- 模块结构遵循 `docs/03-整体架构.md` 的划分，不随意新增顶层目录
- 配置加载与图像处理解耦，引擎不直接读配置文件
- 异步长任务走 `tasks/` 调度，不阻塞请求线程

### 6. 后台程序与端口清理
- 运行测试或验证脚本后，**必须停掉所有本次开启的后台程序与服务**
- 特别注意占用端口的服务器进程（uvicorn 等），验证完立即 kill，不留端口占用
- 确保每次验证结束后环境干净，无残留进程

### 7. 沟通
- 先给结论与建议，再讲理由；不堆砌选项
- 有开放问题或不确定点，明确列出并标优先级，不擅自拍板
- 文件路径用 `file_path:line` 形式引用，便于跳转
